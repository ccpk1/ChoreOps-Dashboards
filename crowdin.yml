# Crowdin Configuration File
# This maps your local file structure to the Crowdin project
# Norwegian translations required mapping because Crowdin uses 'no' (generic) for Google Translate, but Home Assistant requires 'nb' (Bokmål).

preserve_hierarchy: true

files:
  # 1. Dashboard Translations (custom translations directory)
  # Maps: /translations/en_dashboard.json -> /translations/fr_dashboard.json
  - source: /translations/en_dashboard.json
    translation: /translations/%two_letters_code%_dashboard.json
    languages_mapping:
      two_letters_code:
        "no": nb
